<nav class="navbar navbar-expand-lg navbar-mainbg fixed-top">
    <div class="container-fluid">
        <button id="menu-toggle" class="btn btn-primary">
            <i class="fas fa-bars"></i>
        </button>

        <a class="btn btn-light dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="far fa-user"></i>
            <% if (typeof usuario !=='undefined' && usuario) { %>
                <%= usuario.Nombres_Usuario %>
                    <% } else { %>
                        Invitado
                        <% } %>
        </a>
        <ul class="dropdown-menu dropdown-menu-end">
            <% if (typeof usuario !=='undefined' && usuario) { %>
                <li>
                    <a class="dropdown-item">
                        <i class="fas fa-user-circle"></i>
                        <%= usuario.Nombres_Usuario %> <br>
                            <small class="text-muted">Rol: <%= usuario.rol %></small>
                    </a>
                </li>
                <li>
                    <hr class="dropdown-divider">
                </li>
                <li>
                    <a class="dropdown-item text-danger" href="/cerrar">
                        <i class="fas fa-sign-out-alt"></i> Cerrar Sesión
                    </a>
                </li>
                <% } else { %>
                    <li>
                        <a class="dropdown-item" href="/login">
                            <i class="fas fa-sign-in-alt"></i> Iniciar Sesión
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="/registrarse">
                            <i class="fas fa-user-plus"></i> Registrarse
                        </a>
                    </li>
                    <% } %>
        </ul>
    </div>
    </div>
    </div>
</nav>